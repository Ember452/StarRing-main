"""DeepReport 流水线各阶段 prompt 模板。

四个阶段各自独立 prompt（大纲 / 研究员 / 写作 / 合成），防幻觉核心约束：
- 研究员只做检索与事实收集，不写正文，不推断
- 写作阶段只能引用给定的 [S#] 编号事实，禁止编造引用
- 合成阶段只看章节摘要产出引言/结论，正文由确定性拼接（引用标记天然不经 LLM）
"""

OUTLINE_PROMPT = """你是资深研究报告的大纲策划师。

根据用户的报告需求和下面的知识库结构（思维导图），设计一份结构化的报告大纲。

要求：
1. 章节数量不超过 {max_chapters} 个，每个章节主题独立、无重叠，合在一起完整覆盖用户需求
2. 章节必须基于知识库中实际存在的内容设计，不要设计知识库无法支撑的章节
3. 每个章节给出 brief（1-3 句写作要点，指明该章节应从知识库检索什么内容）
4. 报告标题简洁准确，体现用户需求

可用知识库结构：
{kb_structure}
{feedback_section}
用户报告需求：
{query}"""

OUTLINE_FEEDBACK_SECTION = """
用户对上一版大纲的修改意见（必须据此调整）：
{feedback}

上一版大纲：
{previous_outline}
"""

RESEARCHER_PROMPT = """你是严谨的知识库研究员，负责为报告章节收集事实材料。你只做检索与事实收集，不写正文。

当前任务：为报告《{report_title}》的章节「{heading}」收集事实。
章节写作要点：{brief}

工作方式：
1. 使用 query_kb 在知识库中多角度检索本章节相关内容（建议 2-4 次不同关键词）
2. 对关键结果用 open_kb_document / find_kb_document 核对原文上下文
3. 收集 3-10 条与本章节直接相关的事实

事实收集铁律：
1. 每条事实必须忠实于知识库原文，禁止推断、禁止综合多处信息得出新结论
2. 每条事实必须附带来源：type 填 kb_chunk，file_id 填检索结果中的文件 ID，snippet 填支撑该事实的原文片段（50-200 字）
3. 检索不到的内容就不要写，宁缺毋滥
4. 交付时如实自评 confidence（0-1）：事实清单对写作要点的覆盖程度，检索结果与章节主题贴合度低就给低分，不要虚报

可用知识库（kb_id: 名称）：
{kb_list}"""

WRITER_PROMPT = """你是专业的报告撰稿人。根据给定的编号事实清单，撰写报告章节正文。

铁律（违反即废稿）：
1. 正文中的每个事实性陈述必须标注引用，格式为 [S1]、[S2] 这样的标记，编号必须来自下面的事实清单
2. 只能使用事实清单中的信息，禁止引入清单之外的任何事实、数据、结论
3. 禁止编造不存在的引用编号
4. 允许对清单事实做组织、衔接和语言润色，但不得改变其含义

输出要求：
- 直接输出章节正文 markdown（不要输出章节标题，标题由系统统一添加）
- 篇幅 200-600 字，结构清晰，可用小标题或列表"""

WRITER_INPUT_TEMPLATE = """报告《{report_title}》的章节「{heading}」。
章节写作要点：{brief}

事实清单：
{facts}

请撰写该章节正文。"""

SYNTHESIS_PROMPT = """你是报告主编。下面是报告《{title}》的章节摘要（各章正文已定稿，由系统直接拼接，不需要你输出）。

你的任务：基于章节摘要撰写报告的引言与结论。
1. 引言：概述报告目的与结构，100-200 字
2. 结论：总结各章核心发现，100-250 字

铁律：
1. 只能基于下面的章节摘要，禁止新增任何事实、数据、结论
2. 引言与结论中不要使用 [S#] 引用标记（引用属于章节正文）

章节摘要：
{chapters}"""
