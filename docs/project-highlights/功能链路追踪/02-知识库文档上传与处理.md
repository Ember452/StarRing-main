# 知识库文档上传与处理链路追踪（完整版）

> **链路概览**：用户在前端上传文件 → 后端接收并上传到 MinIO → 创建文档记录 → 异步任务解析文件 → 切割为 chunks → 向量化并入库到向量数据库

本文档按照**真实的代码调用顺序**，精确到每个方法名和行号，帮助学习者彻底理解知识库文档处理的完整链路。

---

## 一、完整链路追踪（按方法调用顺序）

### 阶段 1：前端上传文件

**用户操作**：用户在知识库管理页面选择文件并点击上传

**代码路径**：
- 前端组件：`web/src/components/KnowledgeBaseManager.vue`
- API 调用：`web/src/apis/knowledge_api.js`

**调用链路**：

```
用户选择文件
  ↓
KnowledgeBaseManager.vue:handleUpload()
  ↓
knowledge_api.uploadFile(file, kb_id)
  ↓
POST /api/knowledge/files/upload
```

**关键代码**（`knowledge_api.js`）：

```javascript
uploadFile: (file, kb_id) => {
  const formData = new FormData()
  formData.append('file', file)
  return apiPost(`/api/knowledge/files/upload?kb_id=${kb_id}`, formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}
```

---

### 阶段 2：后端路由层接收上传

**代码路径**：`backend/server/routers/knowledge_router.py:1536-1607`

**方法调用顺序**：

```python
@knowledge.post("/files/upload")
async def upload_file(
    file: UploadFile = File(...),
    kb_id: str | None = Query(None),
    current_user: User = Depends(get_admin_user),
):
```

**调用链路**（按执行顺序）：

#### 步骤 2.1：基础校验

```python
# 行 1543-1544
if not file.filename:
    raise HTTPException(status_code=400, detail="No selected file")

# 行 1546-1547
if kb_id:
    await _ensure_database_supports_documents(kb_id, "文档上传")
```

**关键方法**：`_ensure_database_supports_documents()`（`knowledge_router.py:119-126`）

```python
async def _ensure_database_supports_documents(kb_id: str, operation: str) -> None:
    db_info = await knowledge_base.get_database_info(kb_id)
    if not db_info:
        raise HTTPException(status_code=404, detail=f"知识库 {kb_id} 不存在")
    kb_type = (db_info.get("kb_type") or "").lower()
    kb_class = KnowledgeBaseFactory.get_kb_class(kb_type)
    if not kb_class.supports_documents:
        raise HTTPException(status_code=400, detail=f"{db_info.get('name') or kb_type} 只支持检索，不支持{operation}")
```

#### 步骤 2.2：文件类型校验

```python
# 行 1551-1554
ext = os.path.splitext(file.filename)[1].lower()

if not is_supported_file_extension(file.filename):
    raise HTTPException(status_code=400, detail=f"Unsupported file type: {ext}")
```

**关键方法**：`is_supported_file_extension()`（`knowledge/parser/unified.py`）

**支持的文件类型**：`.pdf`, `.docx`, `.txt`, `.md`, `.json`, `.csv`, `.xlsx`, `.pptx` 等

#### 步骤 2.3：读取文件内容（带大小限制）

```python
# 行 1560-1567
try:
    file_bytes = await read_upload_with_limit(
        file,
        max_size_bytes=MAX_UPLOAD_SIZE_BYTES,  # 100MB
        too_large_message="文件过大，当前仅支持 100 MB 以内的文件",
    )
except ValueError as e:
    raise HTTPException(status_code=400, detail=str(e))
```

**关键方法**：`read_upload_with_limit()`（`utils/upload_utils.py`）

**设计亮点**：流式读取，避免大文件占用内存

#### 步骤 2.4：计算内容哈希（去重）

```python
# 行 1569
content_hash = await calculate_content_hash(file_bytes)
```

**关键方法**：`calculate_content_hash()`（`knowledge/utils/__init__.py`）

**设计亮点**：基于文件内容计算哈希，实现内容级去重

#### 步骤 2.5：检查是否已存在（去重）

```python
# 行 1571-1576
file_exists = await knowledge_base.file_existed_in_db(kb_id, content_hash)
if file_exists:
    raise HTTPException(
        status_code=409,
        detail="数据库中已经存在了相同内容文件，File with the same content already exists in this database",
    )
```

**关键方法**：`knowledge_base.file_existed_in_db()`（`knowledge/manager.py`）

#### 步骤 2.6：上传到 MinIO

```python
# 行 1578-1587
timestamp = int(time.time() * 1000)
minio_filename = f"{basename}_{timestamp}{ext}"

bucket_name = MinIOClient.KB_BUCKETS["documents"]
folder = kb_id if kb_id else "unknown"
object_name = f"{folder}/upload/{minio_filename}"

# 上传到MinIO
minio_url = await aupload_file_to_minio(bucket_name, object_name, file_bytes)
```

**关键方法**：`aupload_file_to_minio()`（`storage/minio/client.py`）

**设计亮点**：
- 添加时间戳避免文件名冲突
- 按 `kb_id/upload/` 组织目录结构
- 异步上传，不阻塞主线程

#### 步骤 2.7：检测同名文件

```python
# 行 1589-1591
same_name_files = await knowledge_base.get_same_name_files(kb_id, filename)
has_same_name = len(same_name_files) > 0
```

**设计亮点**：提示用户是否存在同名文件，但不阻止上传（内容不同即可）

#### 步骤 2.8：返回上传结果

```python
# 行 1593-1607
return {
    "message": "File successfully uploaded",
    "file_path": minio_url,
    "minio_path": minio_url,
    "kb_id": kb_id,
    "content_hash": content_hash,
    "filename": filename,
    "original_filename": basename,
    "size": len(file_bytes),
    "minio_filename": minio_filename,
    "object_name": object_name,
    "bucket_name": bucket_name,
    "same_name_files": same_name_files,
    "has_same_name": has_same_name,
}
```

**返回字段**：
- `minio_url` - MinIO 文件路径
- `content_hash` - 内容哈希（用于去重）
- `filename` - 原始文件名
- `same_name_files` - 同名文件列表

---

### 阶段 3：添加文档记录

**用户操作**：用户上传文件后，选择文件并点击"添加到知识库"

**代码路径**：`backend/server/routers/knowledge_router.py:630-810`

**方法调用顺序**：

```python
@knowledge.post("/databases/{kb_id}/documents")
async def add_documents(
    kb_id: str, items: list[str] = Body(...), params: dict = Body(...), current_user: User = Depends(get_admin_user)
):
```

**调用链路**（按执行顺序）：

#### 步骤 3.1：参数校验

```python
# 行 636-638
await _ensure_database_supports_documents(kb_id, "文档添加/解析/入库")

params = _ensure_document_params(params)
content_type = params.get("content_type", "file")
```

#### 步骤 3.2：验证上传文件列表

```python
# 行 656
_validate_uploaded_document_items(items, params)
```

**关键方法**：`_validate_uploaded_document_items()`（`knowledge_router.py:137-163`）

**校验内容**：
- `items` 不能为空
- 每个 item 必须是 MinIO URL
- 必须提供 `content_hash`（用于去重）

#### 步骤 3.3：定义异步任务协程

```python
# 行 658-788
async def run_ingest(context: TaskContext):
    await context.set_message("任务初始化")
    await context.set_progress(5.0, "准备处理文档")

    total = len(items)
    processed_items: list[dict | None] = [None] * total
    added_files: list[dict] = []

    try:
        # 第一阶段：添加文件记录
        await context.set_message("第一阶段：添加文件记录")
        for idx, item in enumerate(items, 1):
            await context.raise_if_cancelled()

            progress = 5.0 + (idx / total) * 25.0
            await context.set_progress(progress, f"[1/3] 添加记录 {idx}/{total}")

            try:
                file_meta = await knowledge_base.add_file_record(
                    kb_id, item, params=params, operator_id=current_user.uid
                )
                added_files.append(
                    {
                        "index": idx - 1,
                        "item": item,
                        "file_id": file_meta["file_id"],
                        "file_meta": file_meta,
                    }
                )
            except Exception as add_error:
                logger.error(f"添加文件记录失败 {item}: {add_error}")
                error_type = "timeout" if isinstance(add_error, TimeoutError) else "add_failed"
                error_msg = "添加超时" if isinstance(add_error, TimeoutError) else "添加记录失败"
                processed_items[idx - 1] = {
                    "item": item,
                    "status": "failed",
                    "error": f"{error_msg}: {str(add_error)}",
                    "error_type": error_type,
                }

        # 第二阶段：解析文件
        await context.set_message("第二阶段：解析文件")
        parse_end = 60.0 if auto_index else 95.0
        parse_total = len(added_files)
        for idx, record in enumerate(added_files, 1):
            await context.raise_if_cancelled()

            progress = 30.0 + (idx / parse_total) * (parse_end - 30.0)
            await context.set_progress(progress, f"[2/3] 解析文件 {idx}/{parse_total}")

            item = record["item"]
            file_id = record["file_id"]
            try:
                file_meta = await knowledge_base.parse_file(kb_id, file_id, operator_id=current_user.uid)
                record["file_meta"] = file_meta
                if not auto_index or file_meta.get("status") != "parsed":
                    processed_items[record["index"]] = file_meta
            except Exception as parse_error:
                logger.error(f"解析文件失败 {item} (file_id={file_id}): {parse_error}")
                error_type = "timeout" if isinstance(parse_error, TimeoutError) else "parse_failed"
                error_msg = "解析超时" if isinstance(parse_error, TimeoutError) else "解析失败"
                processed_items[record["index"]] = {
                    "item": item,
                    "status": "failed",
                    "error": f"{error_msg}: {str(parse_error)}",
                    "error_type": error_type,
                }

        # 第三阶段：自动入库（如果启用）
        if auto_index:
            await context.set_message("第三阶段：自动入库")
            parsed_files = [record for record in added_files if record["file_meta"].get("status") == "parsed"]
            total_parsed = len(parsed_files)

            for idx, record in enumerate(parsed_files, 1):
                await context.raise_if_cancelled()

                progress = 60.0 + (idx / total_parsed) * 35.0
                await context.set_progress(progress, f"[3/3] 入库文件 {idx}/{total_parsed}")

                item = record["item"]
                file_id = record["file_id"]
                try:
                    await knowledge_base.update_file_params(
                        kb_id, file_id, indexing_params, operator_id=current_user.uid
                    )
                    result = await knowledge_base.index_file(
                        kb_id, file_id, operator_id=current_user.uid, params=indexing_params
                    )
                    processed_items[record["index"]] = result
                except Exception as index_error:
                    logger.error(f"自动入库失败 {item} (file_id={file_id}): {index_error}")
                    processed_items[record["index"]] = {
                        "item": item,
                        "status": "failed",
                        "error": f"入库失败: {str(index_error)}",
                        "error_type": "index_failed",
                    }

    except asyncio.CancelledError:
        await context.set_progress(100.0, "任务已取消")
        raise
    except Exception as task_error:
        logger.exception(f"Task processing failed: {task_error}")
        await context.set_progress(100.0, f"任务处理失败: {str(task_error)}")
        raise

    final_items = [
        item
        if item is not None
        else {
            "item": items[index],
            "status": "failed",
            "error": "文件未处理",
            "error_type": "not_processed",
        }
        for index, item in enumerate(processed_items)
    ]
    failed_count = len([item for item in final_items if "error" in item or item.get("status") == "failed"])

    summary = {
        "kb_id": kb_id,
        "item_type": "文件",
        "submitted": total,
        "failed": failed_count,
    }
    message = f"文件处理完成，失败 {failed_count} 个" if failed_count else "文件处理完成"
    await context.set_result(summary | {"items": final_items})
    await context.set_progress(100.0, message)

    if failed_count:
        raise RuntimeError(message)

    return summary | {"items": final_items}
```

**设计亮点**：
- 三阶段处理：添加记录 → 解析文件 → 自动入库
- 每个阶段都更新进度，前端可实时展示
- 支持取消操作（`context.raise_if_cancelled()`）
- 错误隔离：单个文件失败不影响其他文件

#### 步骤 3.4：入队异步任务

```python
# 行 790-807
try:
    database = await knowledge_base.get_database_info(kb_id)
    task = await tasker.enqueue(
        name=f"知识库文档处理 ({database['name']})",
        task_type="knowledge_ingest",
        payload={
            "kb_id": kb_id,
            "items": items,
            "params": params,
            "content_type": content_type,
        },
        coroutine=run_ingest,
    )
    return {
        "message": "任务已提交，请在任务中心查看进度",
        "status": "queued",
        "task_id": task.id,
    }
except Exception as e:
    logger.error(f"Failed to enqueue {content_type}s: {e}, {traceback.format_exc()}")
    raise HTTPException(status_code=500, detail=f"Failed to enqueue task: {e}")
```

**关键方法**：`tasker.enqueue()`（`services/task_service.py`）

**设计亮点**：
- 异步任务，不阻塞 HTTP 请求
- 任务中心可查看进度
- 支持任务取消

---

### 阶段 4：添加文件记录

**代码路径**：`backend/package/starring/knowledge/manager.py` → `knowledge/base.py`

**方法调用顺序**：

```python
async def add_file_record(
    self,
    kb_id: str,
    file_path: str,
    params: dict | None = None,
    operator_id: str | None = None,
) -> dict:
```

**调用链路**（按执行顺序）：

#### 步骤 4.1：获取知识库实例

```python
kb_instance = await self._get_kb_for_database(kb_id)
```

**关键方法**：`_get_kb_for_database()`（`knowledge/manager.py:114-140`）

```python
async def _get_kb_for_database(self, kb_id: str) -> KnowledgeBase:
    from starring.repositories.knowledge_base_repository import KnowledgeBaseRepository

    kb_repo = KnowledgeBaseRepository()
    kb = await kb_repo.get_by_kb_id(kb_id)

    if kb is None:
        raise KBNotFoundError(f"Database {kb_id} not found")

    kb_type = kb.kb_type or "milvus"

    if not KnowledgeBaseFactory.is_type_supported(kb_type):
        raise KBNotFoundError(f"Unsupported knowledge base type: {kb_type}")

    return self._get_or_create_kb_instance(kb_type)
```

#### 步骤 4.2：创建文件记录

```python
file_meta = await kb_instance.add_file_record(
    kb_id,
    file_path,
    params=params,
    operator_id=operator_id,
)
```

**关键方法**：`KnowledgeBase.add_file_record()`（`knowledge/base.py`）

**数据库操作**：
- 在 `knowledge_files` 表创建记录
- 状态为 `pending`（待解析）
- 存储文件路径、元数据、创建者等信息

---

### 阶段 5：解析文件

**代码路径**：`backend/package/starring/knowledge/base.py`

**方法调用顺序**：

```python
async def parse_file(
    self,
    kb_id: str,
    file_id: str,
    operator_id: str | None = None,
) -> dict:
```

**调用链路**（按执行顺序）：

#### 步骤 5.1：获取文件记录

```python
file_record = await self._get_file_record(kb_id, file_id)
if not file_record:
    raise ValueError(f"File {file_id} not found in {kb_id}")
```

#### 步骤 5.2：从 MinIO 下载文件

```python
file_path = file_record.meta.get("path")
if not file_path:
    raise ValueError("File path not found in metadata")

# 从 MinIO 下载到临时目录
temp_dir = tempfile.mkdtemp()
local_path = await self._download_from_minio(file_path, temp_dir)
```

**关键方法**：`_download_from_minio()`（`knowledge/base.py`）

#### 步骤 5.3：调用统一 Parser 解析

```python
# 调用统一解析器
markdown_content = await Parser.aparse(local_path)
```

**关键方法**：`Parser.aparse()`（`knowledge/parser/unified.py`）

**解析流程**：
1. 根据文件类型选择解析器（PDF/DOCX/PPTX 等）
2. 调用对应解析器提取文本内容
3. 转换为 Markdown 格式
4. 返回解析结果

**支持的解析器**：
- `MinerU` - 高质量 PDF 解析
- `PP-Structure` - 表格和结构识别
- `DeepSeek OCR` - OCR 识别
- `docling` - 轻量级文档解析

#### 步骤 5.4：上传解析结果到 MinIO

```python
# 上传解析后的 Markdown 到 MinIO
parsed_md_path = f"{kb_id}/parsed/{file_id}.md"
minio_url = await aupload_file_to_minio(
    MinIOClient.KB_BUCKETS["parsed"],
    parsed_md_path,
    markdown_content.encode("utf-8"),
)
```

#### 步骤 5.5：更新文件记录

```python
# 更新文件状态为 parsed
await self._update_file_record(
    kb_id,
    file_id,
    status="parsed",
    meta={
        **file_record.meta,
        "parsed_path": minio_url,
        "parsed_at": utc_isoformat(),
    },
)
```

**状态转换**：`pending` → `parsed`

---

### 阶段 6：切割为 Chunks

**代码路径**：`backend/package/starring/knowledge/chunking/ragflow_like/dispatcher.py`

**方法调用顺序**：

```python
def dispatch_chunks(
    markdown_content: str,
    chunk_preset: str = "general",
    chunk_size: int = 500,
    chunk_overlap: int = 50,
) -> list[dict]:
```

**调用链路**（按执行顺序）：

#### 步骤 6.1：选择切割策略

```python
preset = get_preset(chunk_preset)
parser = preset.get_parser()
```

**支持的切割策略**：
- `general` - 通用切割（按段落和标题）
- `qa` - 问答对切割
- `semantic` - 语义切割（基于句子相似度）
- `laws` - 法律条文切割
- `book` - 书籍切割（按章节）

#### 步骤 6.2：执行切割

```python
chunks = parser.parse(
    markdown_content,
    chunk_size=chunk_size,
    chunk_overlap=chunk_overlap,
)
```

**切割逻辑**：
1. 按 Markdown 标题分割章节
2. 按段落分割内容
3. 合并短段落，拆分长段落
4. 添加重叠区域（避免语义断裂）

#### 步骤 6.3：生成 Chunk 元数据

```python
for idx, chunk in enumerate(chunks):
    chunk["chunk_id"] = f"{file_id}_{idx}"
    chunk["file_id"] = file_id
    chunk["kb_id"] = kb_id
    chunk["index"] = idx
```

---

### 阶段 7：向量化并入库

**代码路径**：`backend/package/starring/knowledge/base.py`

**方法调用顺序**：

```python
async def index_file(
    self,
    kb_id: str,
    file_id: str,
    operator_id: str | None = None,
    params: dict | None = None,
) -> dict:
```

**调用链路**（按执行顺序）：

#### 步骤 7.1：获取解析后的 Markdown

```python
file_record = await self._get_file_record(kb_id, file_id)
parsed_path = file_record.meta.get("parsed_path")

# 从 MinIO 下载解析后的 Markdown
markdown_content = await self._download_parsed_markdown(parsed_path)
```

#### 步骤 7.2：切割为 Chunks

```python
chunk_preset = (params or {}).get("chunk_preset_id", "general")
chunk_size = (params or {}).get("chunk_size", 500)
chunk_overlap = (params or {}).get("chunk_overlap", 50)

chunks = dispatch_chunks(
    markdown_content,
    chunk_preset=chunk_preset,
    chunk_size=chunk_size,
    chunk_overlap=chunk_overlap,
)
```

#### 步骤 7.3：调用 Embedding 模型

```python
# 批量调用 Embedding 模型
texts = [chunk["content"] for chunk in chunks]
embeddings = await self._embed_texts(texts)
```

**关键方法**：`_embed_texts()`（`knowledge/base.py`）

**设计亮点**：
- 批量调用，减少 API 调用次数
- 自动重试（网络瞬断）
- 支持多种 Embedding 模型

#### 步骤 7.4：写入向量数据库

```python
# 写入向量数据库（Milvus）
await self._insert_vectors(
    kb_id=kb_id,
    chunks=chunks,
    embeddings=embeddings,
)
```

**关键方法**：`_insert_vectors()`（`knowledge/implementations/milvus.py`）

**数据库操作**：
- 在 Milvus 集合中插入向量
- 存储 chunk 元数据（file_id、chunk_id、content 等）
- 自动分配 chunk ID

#### 步骤 7.5：更新文件状态

```python
# 更新文件状态为 indexed
await self._update_file_record(
    kb_id,
    file_id,
    status="indexed",
    meta={
        **file_record.meta,
        "chunk_count": len(chunks),
        "indexed_at": utc_isoformat(),
    },
)
```

**状态转换**：`parsed` → `indexed`

---

### 阶段 8：更新统计信息

**代码路径**：`backend/package/starring/knowledge/base.py`

**方法调用顺序**：

```python
await self._update_file_stats(kb_id, file_id)
```

**调用链路**（按执行顺序）：

#### 步骤 8.1：计算 Token 数

```python
token_count = sum(len(chunk["content"].split()) for chunk in chunks)
```

#### 步骤 8.2：更新数据库统计

```python
from starring.repositories.knowledge_file_repository import KnowledgeFileRepository

file_repo = KnowledgeFileRepository()
await file_repo.update_stats(
    file_id=file_id,
    chunk_count=len(chunks),
    token_count=token_count,
)
```

---

## 二、设计亮点总结

### 2.1 三阶段异步处理

| 阶段 | 职责 | 进度范围 |
|------|------|----------|
| 添加记录 | 创建文件记录，状态为 `pending` | 5% - 30% |
| 解析文件 | 调用 Parser 提取文本，状态为 `parsed` | 30% - 60% |
| 自动入库 | 切割、向量化、写入向量库，状态为 `indexed` | 60% - 100% |

**优势**：
- 每个阶段独立，可单独重试
- 实时进度反馈，用户体验好
- 支持取消操作

### 2.2 内容级去重

基于文件内容计算哈希，而非文件名，避免重复上传相同内容的文件。

**实现**：
```python
content_hash = await calculate_content_hash(file_bytes)
file_exists = await knowledge_base.file_existed_in_db(kb_id, content_hash)
```

### 2.3 统一 Parser 抽象

支持多种文档格式（PDF/DOCX/PPTX 等），通过工厂模式选择解析器。

**设计**：
```python
markdown_content = await Parser.aparse(local_path)
```

**优势**：
- 上层代码无需关心具体格式
- 易于扩展新格式
- 支持降级（docling → 轻量级库）

### 2.4 灵活的切割策略

支持多种切割策略（通用/问答/语义/法律/书籍），通过预设配置选择。

**设计**：
```python
chunks = dispatch_chunks(
    markdown_content,
    chunk_preset="general",
    chunk_size=500,
    chunk_overlap=50,
)
```

### 2.5 批量向量化

批量调用 Embedding 模型，减少 API 调用次数，提升性能。

**设计**：
```python
embeddings = await self._embed_texts(texts)  # 批量调用
```

### 2.6 错误隔离

单个文件失败不影响其他文件，任务继续执行。

**设计**：
```python
try:
    file_meta = await knowledge_base.parse_file(kb_id, file_id)
except Exception as parse_error:
    processed_items[idx] = {"status": "failed", "error": str(parse_error)}
```

---

## 三、主要功能

### 3.1 文件上传

- 支持多种文件格式（PDF/DOCX/PPTX/TXT/MD 等）
- 文件大小限制（100MB）
- 内容级去重
- 上传到 MinIO 对象存储

### 3.2 文档解析

- 统一 Parser 抽象
- 支持多种解析器（MinerU/PP-Structure/DeepSeek OCR/docling）
- 转换为 Markdown 格式
- 上传解析结果到 MinIO

### 3.3 文档切割

- 支持多种切割策略（通用/问答/语义/法律/书籍）
- 可配置 chunk 大小和重叠区域
- 保留 Markdown 结构信息

### 3.4 向量化入库

- 批量调用 Embedding 模型
- 写入向量数据库（Milvus）
- 更新文件状态和统计信息

### 3.5 异步任务

- 三阶段异步处理
- 实时进度反馈
- 支持取消操作
- 错误隔离

---

## 四、可改进之处

### 4.1 Parser 性能瓶颈

**问题**：某些解析器（如 MinerU）解析速度较慢，大文件可能需要数分钟。

**改进建议**：
- 增加解析超时控制
- 支持断点续传（已解析的章节跳过）
- 考虑使用缓存（相同内容不重复解析）

**代码位置**：`backend/package/starring/knowledge/parser/unified.py`

### 4.2 切割策略不够智能

**问题**：当前切割策略主要基于规则（标题/段落），缺乏语义理解。

**改进建议**：
- 引入语义切割（基于句子相似度）
- 支持自定义切割规则
- 提供切割预览功能

**代码位置**：`backend/package/starring/knowledge/chunking/ragflow_like/dispatcher.py`

### 4.3 向量化批量大小未优化

**问题**：批量调用 Embedding 模型时，批量大小固定，未根据模型限制优化。

**改进建议**：
- 根据模型限制动态调整批量大小
- 支持并发调用（多个批量并行处理）
- 增加重试机制（网络瞬断）

**代码位置**：`backend/package/starring/knowledge/base.py`

### 4.4 缺少解析质量评估

**问题**：解析后的 Markdown 质量未评估，可能存在解析错误。

**改进建议**：
- 增加解析质量评估（OCR 置信度、表格识别准确率等）
- 提供解析结果预览和编辑功能
- 支持手动修正解析错误

**代码位置**：`backend/package/starring/knowledge/parser/unified.py`

### 4.5 缺少增量更新

**问题**：文件更新时需要重新解析和入库，不支持增量更新。

**改进建议**：
- 检测文件变更（基于内容哈希）
- 仅更新变更的 chunks
- 保留历史版本

**代码位置**：`backend/package/starring/knowledge/base.py`

---

## 五、代码路径索引

| 模块 | 文件路径 | 职责 |
|------|----------|------|
| 后端路由 | `backend/server/routers/knowledge_router.py` | 文件上传、文档管理路由 |
| 知识库管理器 | `backend/package/starring/knowledge/manager.py` | 统一管理知识库实例 |
| 知识库基类 | `backend/package/starring/knowledge/base.py` | 知识库抽象基类 |
| 知识库工厂 | `backend/package/starring/knowledge/factory.py` | 创建知识库实例 |
| 统一 Parser | `backend/package/starring/knowledge/parser/unified.py` | 文档解析为 Markdown |
| 切割调度器 | `backend/package/starring/knowledge/chunking/ragflow_like/dispatcher.py` | 切割策略调度 |
| Milvus 实现 | `backend/package/starring/knowledge/implementations/milvus.py` | Milvus 向量库实现 |
| MinIO 客户端 | `backend/package/starring/storage/minio/client.py` | 对象存储客户端 |
| 任务服务 | `backend/package/starring/services/task_service.py` | 异步任务管理 |
| 数据访问 | `backend/package/starring/repositories/knowledge_file_repository.py` | 文件记录数据库操作 |

---

**文档更新日期**: 2026-07-23  
**文档版本**: v3.0（按方法调用顺序重写）  
**维护者**: StarRing 项目组
